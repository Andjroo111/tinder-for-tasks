{
  "name": "tinder-for-tasks",
  "module": "server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch --env-file=$HOME/.credentials/mcp-keys/all-keys.env server.ts",
    "start": "bun --env-file=$HOME/.credentials/mcp-keys/all-keys.env server.ts",
    "seed": "bun --env-file=$HOME/.credentials/mcp-keys/all-keys.env scripts/seed.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.12.12"
  }
}
